<div class="col">
			<h2>User info</h2>
			<!-- <%  %> -->
			<table>
				<tbody>
					<tr>
						<th>name</th>
						<th><%= current_user.name %></th>
					</tr>
					<tr>
						<th>introduction</th>
						<p><%= link_to 'Edit', edit_user_path(@user) %></p>
					</tr>
				</tbody>
			</table>

			<h2>New book</h2>
			<%= form_for @book do |f| %>

			<p>Title</p>
			<%= f.text_field :title %>

			<p>Opinion</p>
			<%= f.text_area :body %>

			<br>
			<%= f.submit 'Create Book' %>
			<% end %>
		</div>